# Legacy Location

This folder contains older preparedness answer artifacts.

Canonical output location is now repository-root `answers/<model_class>/` (for example `answers/claude/`).
Use `docs/theory/guardian_preparedness/prompt.json` as the single source of generation instructions.
